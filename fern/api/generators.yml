release:
  - name: fernapi/fern-typescript-sdk
    version: 0.0.222
    publishing:
      npm:
        package-name: '@fern-api/raven' # TODO: change to Raven's NPM org
        
        # TODO: add as an environment variable in Github Secrets
        # https://github.com/organizations/ravenappdev/settings/secrets/actions
        token: ${RAVEN_NPM_TOKEN} 
        
    github:
      repository: ravenappdev/raven-node
  - name: fernapi/fern-java-sdk
    version: 0.0.122
    publishing:
      maven:
        coordinate: io.github.fern-api:raven
        username: ${RAVEN_MAVEN_USERNAME} # TODO: add as an environment variable in Github Secrets
        password: ${RAVEN_MAVEN_TOKEN} # TODO: add as an environment variable in Github Secrets
    github:
      repository: ravenappdev/raven-java
  - name: fernapi/fern-openapi
    version: 0.0.10
    github:
      repository: ravenappdev/raven-openapi
