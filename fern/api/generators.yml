release:
  - name: fernapi/fern-typescript-sdk
    version: 0.0.209
    publishing:
      npm:
        package-name: "@ravenapp/raven-node"
        token: ${RAVEN_NPM_TOKEN} # TODO: should use Raven's NPM Token
    github:
      repository: ravenappdev/raven-node
